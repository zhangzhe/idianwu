<div class="dl-horizontal">
	<dd>
	<h1>
		<%= link_to_unless_current @item.name, @item %>
	</h1>
	</dd>
</div>
<dl class="dl-horizontal">
	<dd>
		<%= link_to 'edit', edit_item_path(@item), :class => 'btn' %>
		<%= link_to 'destroy', item_path(@item), :method => 'delete', :confirm => 'Are you sure?', :class => 'btn btn-danger' %>
	</dd>
	<dd>
		<br>
		<br>
	</dd>
	<dd>
		<%= raw g_textile(@item.description) %>
	</dd>
</dl>
